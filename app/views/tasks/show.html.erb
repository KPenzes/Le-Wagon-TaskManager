<h1><%= @task.title %></h1>

<p><%= @task.details %></p>
<p>Completed?<%= @task.completed %></p>

<%= link_to "Delete", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete?"} %>
<%= link_to "Index", tasks_path %>
<%= link_to "Edit", task_edit_path(@task) %>
